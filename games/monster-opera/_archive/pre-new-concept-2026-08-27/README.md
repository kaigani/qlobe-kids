# Archived module snapshot

This directory preserves the retired pre-rebuild files for repository history only. Nothing here is registered, imported, scanned as a game manifest, or referenced by the current runtime.
