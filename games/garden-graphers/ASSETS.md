# Garden Graphers assets

Garden Graphers is a Watercolor / Storybook game. The accepted environment,
journal, title, and carrier masters are GPT Image 2 raster outputs retained in
`assets/source/gpt-image-2/`. Runtime sprites and carriers were made by
deterministic contact-sheet slicing, trimming, resizing, and WebP encoding.
The recorded Qwen Image Layered attempts were unavailable, so the provenance
records identify the local contiguous corner-matte extraction fallback and its
magenta QA composites.

The hub tile source is retained at `assets/source/gpt-image-2/hub-tile-source.png`;
the curated 640x533 JPEG is `../../assets/hub/tiles/garden-graphers.jpg`. The
Krea 2 `menu-game-tile` attempt is recorded as failed (job `a6989ad355e4`, host
unavailable); the accepted replacement is a separately prompted GPT Image 2
hub render. Both exact requests are retained in
`assets/source/gpt-image-2/prompts.json`.

Ari's canonical notice and celebrate sources are retained unchanged below
`assets/source/canonical/`. GPT Image 2 edit passes preserve those poses and
identity while repainting their glossy rendering as watercolor/gouache storybook
sprites. Exact edit prompts and source outputs are retained beside the other GPT
masters; deterministic checker-matte extraction produces the transparent runtime
WebPs and magenta QA composites. The three graph category keys are deterministic
raster compositions of accepted visitor art and the painted green/gold carrier,
with the tiny icon embedded in a large named label so it cannot read as another
countable graph unit.

Teacher TTS generation and Whisper QA could not read the configured reference
file, so the recorded runtime manifest is empty and the game uses exact
device-speech fallbacks. The shared gentle-country-morning BGM is used by the
game platform after gesture unlock and follows mute/ducking.

No LAN URL, private source path, voice reference, or credentials are included
in this record. Source masters and QA evidence remain for reproducibility.
